Excellent. ✅ **The application is running successfully on EKS.**

Your results prove several important things:

### Current status

| Component       | Status           |
| --------------- | ---------------- |
| Namespace       | `Active`         |
| Deployment      | `2/2 Available`  |
| ReplicaSet      | `2/2 Ready`      |
| Pod 1           | `Running`, `1/1` |
| Pod 2           | `Running`, `1/1` |
| Restarts        | `0`              |
| Readiness probe | Passing          |
| Liveness probe  | Configured       |
| ECR image pull  | Successful       |
| LoadBalancer    | Provisioned      |

The Pods are both currently on the same worker node:

```text
ip-10-0-12-151.ap-south-1.compute.internal
```

That's not an error, but **we'll improve this later** so replicas can be distributed across nodes/AZs for better resilience.

Also, this event:

```text
FailedCreatePodSandBox ... failed to assign an IP address
```

was transient. The Pod subsequently started successfully, so the AWS VPC CNI recovered. We don't need to troubleshoot it right now.

---

# Phase 5.4 — Test the application through the Load Balancer

We now have the exciting part: let's test the application from outside Kubernetes.

Your LoadBalancer DNS is:

```text
ab66c1b8938c84ec3901833db5e73b89-184645698.ap-south-1.elb.amazonaws.com
```

### 1. Test `/health`

Run:

```powershell
curl.exe http://ab66c1b8938c84ec3901833db5e73b89-184645698.ap-south-1.elb.amazonaws.com/health
```

Expected:

```json
{"status":"healthy"}
```

### 2. Test `/version`

```powershell
curl.exe http://ab66c1b8938c84ec3901833db5e73b89-184645698.ap-south-1.elb.amazonaws.com/version
```

Expected:

```json
{
  "application": "DevOps Demo Application",
  "environment": "development",
  "version": "1.0.0"
}
```

### 3. Test the home page

```powershell
curl.exe http://ab66c1b8938c84ec3901833db5e73b89-184645698.ap-south-1.elb.amazonaws.com/
```

You should receive the HTML from `index.html`.

---

## One important production observation

Notice that your `/version` endpoint still says:

```text
environment: development
```

even though the application is now running in AWS/EKS.

**That's actually a useful thing to fix later.**

We'll eventually make the environment configurable through Kubernetes configuration rather than hard-coded in Python.

Similarly, we'll eventually move from:

```text
image: ...:1.0.0
```

to an automated immutable version/tag generated by CI/CD.

But **don't change either yet**. We're intentionally validating the current architecture first.

---

## Our architecture has now reached this point

```text
                    GitHub
                       │
                       │
                  Docker Build
                       │
                       ▼
                      ECR
                       │
                image: 1.0.0
                       │
                       ▼
               ┌──────────────┐
               │     EKS      │
               │              │
               │  Deployment  │
               │    2 Pods    │
               │      │       │
               └──────┼───────┘
                      │
                Kubernetes
                  Service
                      │
                      ▼
              AWS Load Balancer
                      │
                      ▼
                   Internet
```

**Run the three `curl.exe` commands and send me their outputs.**

After that, we'll move into an important Kubernetes exercise: **rolling update and rollback**. That will let us demonstrate the "CD" part of the project manually before we automate it with GitHub Actions.
